//! `demon-tui` — the terminal client, refactored from proximiio-tui to render
//! server-pushed `demon-core` view models instead of polling SSH directly.
//! Placeholder; becomes a thin client in Phase 1.
#![forbid(unsafe_code)]
