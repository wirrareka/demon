//! `demon-clients` — typed API clients for the services demon controls (kalista,
//! vulture, identity, and the future vault broker). Adapted from proximiio-tui's
//! `*_api.rs`. Placeholder; populated in Phase 1 (read) and Phase 2 (auth).
#![forbid(unsafe_code)]
